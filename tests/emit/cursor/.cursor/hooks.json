{
  "_provenance": "emitted by ticketwright install v3.9.0 \u2014 do not hand-edit; re-run `ticketwright install --runtime cursor` to update.",
  "_schema_note": "hook-entry shape assembled from the documented fields; the config file's full schema is live-unverified \u2014 see the enforcement table in AGENTS.md.",
  "hooks": {
    "beforeShellExecution": [
      {
        "command": "bash bin/tw hook_shim.py --runtime cursor --hook shell_guards",
        "failClosed": true
      }
    ]
  }
}
