{
  "_provenance": "emitted by ticketwright install v4.0.3 \u2014 do not hand-edit; re-run `ticketwright install --runtime antigravity` to update.",
  "_schema_note": "hook-entry shape assembled from the documented fields; the config file's full schema is live-unverified \u2014 see the enforcement table in AGENTS.md.",
  "hooks": {
    "PreToolUse": [
      {
        "command": "bash bin/tw hook_shim.py --runtime antigravity --hook shell_guards"
      }
    ],
    "PostToolUse": [
      {
        "command": "bash bin/tw hook_shim.py --runtime antigravity --hook regenerate_ticket_index"
      }
    ]
  }
}
