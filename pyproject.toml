[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "workwright"
dynamic = ["version"]  # single source of truth: workwright/__init__.py (read by [tool.hatch.version])
description = "Tool-agnostic AI layer for ticket-driven work repos: prior-art recall, object reverse-index, deep QC, and a self-maintaining ticket index. Stdlib-only, no vector store."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "Kyle Chalmers" }]
keywords = ["tickets", "claude-code", "recall", "ticket-index", "qc", "workflow", "jira", "linear", "snowflake"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Topic :: Software Development :: Code Generators",
]
# Zero runtime dependencies — the kit is deliberately stdlib-only.
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8", "build>=1", "twine>=5"]

[project.urls]
Homepage = "https://github.com/kyle-chalmers/workwright"
Repository = "https://github.com/kyle-chalmers/workwright"
Issues = "https://github.com/kyle-chalmers/workwright/issues"

[project.scripts]
workwright = "workwright.cli:main"

[tool.hatch.version]
path = "workwright/__init__.py"

[tool.hatch.build.targets.wheel]
packages = ["workwright"]

# Bundle the kit assets into the wheel under workwright/_kit/ WITHOUT moving them in the repo —
# the Claude Code plugin (.claude-plugin/) and `cp -r` install still reference bin/ at the repo root.
[tool.hatch.build.targets.wheel.force-include]
"bin" = "workwright/_kit/bin"
".claude/commands" = "workwright/_kit/.claude/commands"
".claude/skills" = "workwright/_kit/.claude/skills"
".claude/agents" = "workwright/_kit/.claude/agents"
".claude/hooks" = "workwright/_kit/.claude/hooks"
".claude/config" = "workwright/_kit/.claude/config"
".claude/settings.json.tmpl" = "workwright/_kit/.claude/settings.json.tmpl"
".claude/statusline.sh" = "workwright/_kit/.claude/statusline.sh"
"adapters" = "workwright/_kit/adapters"
"templates" = "workwright/_kit/templates"

[tool.hatch.build.targets.sdist]
include = ["workwright", "bin", ".claude", "adapters", "templates", "README.md", "LICENSE", "CHANGELOG.md"]
