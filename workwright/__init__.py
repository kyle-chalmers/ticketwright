"""Workwright — a tool-agnostic AI layer for ticket-driven work repos.

This package is the PyPI distribution: a stdlib-only CLI that scaffolds the kit into a repo
(`workwright init`) and runs the prior-art recall + ticket-index tools standalone
(`workwright recall` / `index`). The kit assets (.claude/, adapters/, templates/, bin/) are
bundled under `workwright/_kit/`. The Claude Code plugin and `cp -r` install paths are unchanged.
"""

__version__ = "4.0.0"
