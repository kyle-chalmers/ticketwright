"""Ticketwright — a tool-agnostic AI layer for ticket-driven work repos.

This package is the PyPI distribution: a stdlib-only CLI that scaffolds the kit into a repo
(`ticketwright init`) and runs the prior-art recall + ticket-index tools standalone
(`ticketwright recall` / `index`). The kit assets (.claude/, adapters/, templates/, bin/) are
bundled under `ticketwright/_kit/`. The Claude Code plugin and `cp -r` install paths are unchanged.
"""

__version__ = "1.3.1"
